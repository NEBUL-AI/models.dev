# Reasoning on this host: GET /v1/model/info marks this served ID with
# supports_reasoning = false and exposes no reasoning_efforts for it
# (https://api.inference.nebul.io/v1/model/info, checked 2026-09-22). Per
# Nebul's docs that catalog is the source of truth for reasoning-capable
# models, and the reasoning guide does not list Kimi among the
# trace-returning families
# (https://docs.nebul.io/docs/inference-api/advanced-topics/reasoning).
# Nebul serves this ID with thinking disabled — no reasoning controls and no
# reasoning_content traces — so override the inherited lab default with
# reasoning = false and declare no reasoning_options / interleaved.
base_model = "moonshotai/kimi-k3"

reasoning = false

[cost]
input = 4.73
output = 23.63
cache_read = 1.13
